- Why nodejs?
  nodejs là môi trường, platform chạy js bên phía server
  nodejs không phải ngôn ngữ

- Set up
nodejs, chrome, git, vscode

- Client/ Server?
  khi truy cập trang web, hành động gì đó web sẽ gửi request lên server => sau đó server trả về kết quả response giao diện, html/css/js ...
  localhost

- NPM
  Quản lí các thư viện js đang sử dụng

- Express App

- View Engines
 ejs
 install babel  để biên dịch code js mới 
 nodemon để restart App
 viewEngine là nơi viết code giao diện html của web App
 config cách render 1 cách dễ dàng, tất cả render từ views/...